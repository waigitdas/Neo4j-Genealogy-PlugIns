//store this file as C:\\Genealogy\Neo4j\neo4j.wai
project| {project name}
user_database| {database name}
neo4j_user name| neo4j
neo4j_password| {neo4j password}
neo4j_import_directory| { your import directory. Use / separator and NOT \. End with /}
root_directory| {root directory for DNA data uoloads}
gedcom_file| {full path to gedcom file. Use /
Curated_rn_gedcom_file| {file name; file in root directory}. Only needed for DNA uploads
tg_file| {currated triangulation group files}
